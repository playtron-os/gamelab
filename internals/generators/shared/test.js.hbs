import renderer from 'react-test-renderer';
import { {{ pascalCase name }} } from '.';

it('{{ pascalCase name }} - renders correctly', () => {
  const tree = renderer
    .create(<{{ pascalCase name}} />)
    .toJSON();
  expect(tree).toMatchSnapshot();
});
